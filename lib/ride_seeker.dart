import 'package:flutter/material.dart';

class RideSeeker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Ride Seeker'),
    );
  }
}
